openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
  description: something
paths:
  /api/something/v1:
    get:
      parameters:
        - name: "startDateTime"
          schema:
            type: string
          in: "query"
        - name: "EndDateTime"
          schema:
            type: string
          in: "query"
      responses:
        "200":
          description: "something"
          content:
            application/json: {}
