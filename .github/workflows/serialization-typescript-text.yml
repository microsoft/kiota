name: Typescript Serialization Text

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: ['serialization/typescript/text/**', '.github/workflows/**']
  pull_request:
    paths: ['serialization/typescript/text/**', '.github/workflows/**']

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      relativePath: ./serialization/typescript/text
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          always-auth: true
          node-version: '16'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@microsoft'
      - run: npm ci
        working-directory: ${{ env.relativePath }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: npm run build
        working-directory: ${{ env.relativePath }}
      # - run: npm run test
      #   working-directory: ${{ env.relativePath }}
      - uses: actions/upload-artifact@v2
        with:
          name: drop
          path: |
            ${{ env.relativePath }}/dist/
            ${{ env.relativePath }}/.npmignore
            ${{ env.relativePath }}/README.md
            ${{ env.relativePath }}/package.json
            ${{ env.relativePath }}/package-lock.json
  deploy:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    environment:
      name: staging_feeds
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@microsoft'
      - uses: actions/download-artifact@v2
        with:
          name: drop
      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.PUBLISH_GH_TOKEN }}" > .npmrc
      - run: npm publish
      - run: rm .npmrc
        if: always()
