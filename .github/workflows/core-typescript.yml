name: Typescript Core

on:
  push:
    branches: [ main ]
    paths: ['core/typescript/**', '.github/workflows/**']
  pull_request:
    paths: ['core/typescript/**', '.github/workflows/**']

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      relativePath: ./core/typescript
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: |
          echo "@microsoft:registry=https://npm.pkg.github.com/microsoft\n//npm.pkg.github.com/:_authToken=${{ secrets.PUBLISH_GH_TOKEN }}" > .npmrc
        working-directory: ${{ env.relativePath }}
      - run: npm ci
        working-directory: ${{ env.relativePath }}
      - run: rm .npmrc
        if: always()
        working-directory: ${{ env.relativePath }}
      - run: npm run build
        working-directory: ${{ env.relativePath }}
      # - run: npm run test
      #   working-directory: ${{ env.relativePath }}
      - uses: actions/upload-artifact@v2
        with:
          name: drop
          path: |
            ${{ env.relativePath }}/dist/
            ${{ env.relativePath }}/.npmignore
            ${{ env.relativePath }}/README.md
            ${{ env.relativePath }}/package.json
            ${{ env.relativePath }}/package-lock.json
  deploy:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - uses: actions/download-artifact@v2
        with:
          name: drop
      - run: echo "//npm.pkg.github.com/:_authToken=${{ secrets.PUBLISH_GH_TOKEN }}" > .npmrc
      - run: npm publish
      - run: rm .npmrc
        if: always()
